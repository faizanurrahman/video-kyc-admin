import { GenericResponseModel } from './generic-response.model';

describe('GenericResponseModel', () => {
  it('should create an instance', () => {
    expect(new GenericResponseModel()).toBeTruthy();
  });
});
